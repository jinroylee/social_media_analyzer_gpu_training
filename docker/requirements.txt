# GPU-specific requirements (PyTorch installed separately)
transformers>=4.35.0
peft>=0.6.0
accelerate>=0.24.0

# Essential data processing
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0
pillow>=10.0.0

# AWS dependencies
boto3>=1.29.0
botocore>=1.32.0

# MLflow (required by mlflow_utils.py)
mlflow>=2.8.0

# Text processing (minimal)
regex>=2023.10.3

# Essential utilities only
tqdm>=4.66.0
python-dotenv>=1.0.0 